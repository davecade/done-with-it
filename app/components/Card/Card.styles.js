import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        backgroundColor: "#fff",
        borderRadius: 15,
        marginBottom: 20,
    },
    textContainer: {
        width: "100%",
        padding: 20,
    },
});

export default styles;
